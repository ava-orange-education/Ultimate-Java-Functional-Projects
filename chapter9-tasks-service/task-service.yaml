apiVersion: v1
kind: Service
metadata:
  name: task-service
spec:
  selector:
    app: task-service  # targets pods with this label
  ports:
    - port: 80         # cluster IP port
      targetPort: 8080 # forwards to container's port 8080
      protocol: TCP
  type: ClusterIP      # default type, internal to cluster
