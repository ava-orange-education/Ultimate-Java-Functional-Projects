spring.application.name=task-service

# Example: read connection string from env in Kubernetes, but for local dev you can set it here.
# This mirrors SPRING_DATA_MONGODB_URI from the deployment manifest.
spring.data.mongodb.uri=mongodb://localhost:27017/tasksdb

server.port=8080
